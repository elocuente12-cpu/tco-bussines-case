# 1. Skew real contra el DC (lo más importante)
w32tm /query /status
w32tm /query /source
w32tm /stripchart /computer:gdc.local /samples:3 /dataonly

# 2. ¿El share FSx responde y resuelve?
$fsx = "<fsx_dns_name>"   # el mismo que usa el userdata
Resolve-DnsName $fsx
Test-NetConnection $fsx -Port 445

# 3. Probar el mapeo manualmente con credenciales explícitas para ver el error crudo
$sec = Get-SECSecretValue -SecretId "<secret_id>" -Region us-east-1
$sec.SecretString    # confirmar que username/password están poblados (NO lo pegues aquí, solo verifica que no estén vacíos)
Mientras tanto, mi hipótesis de fix (pendiente de confirmar con lo de arriba) es endurecer el sync de tiempo en la Fase 2 apuntando explícitamente al DC antes del mount, en lugar de confiar en w32tm /resync /force:

powershell

# Apuntar W32Time al DC del dominio y resincronizar de verdad
w32tm /config /manualpeerlist:"gdc.local" /syncfromflags:manual /update
Restart-Service W32Time
w32tm /resync /rediscover /force
# Verificar skew < 5 min antes de intentar el mount