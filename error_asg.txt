# 1. Que hora ve el sistema vs que deberia
Get-Date
[System.TimeZoneInfo]::Local.Id
w32tm /query /status | Select-String "Source|Last"

# 2. El registro real de timezone (esto es lo que necesito ver)
Get-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\TimeZoneInformation"

# 3. Confirmar RealTimeIsUniversal (segun AWS ya deberia estar en 1)
Get-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\TimeZoneInformation" -Name RealTimeIsUniversal

# 4. CLAVE: la hora UTC real vs la local
[System.DateTime]::UtcNow
Get-Date
